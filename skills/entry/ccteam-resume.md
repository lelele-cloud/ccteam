# CCTeam - 恢复项目

## 概述

恢复中断的项目，智能识别项目状态并从上次停止的地方继续。与流程引擎深度集成，支持多种恢复策略。

## 使用方法

当用户调用 `/ccteam-resume` 时：

### 1. 检测项目状态文件

```javascript
function checkProjectStatus() {
  const statusFile = "docs/ccteam/project-status.json";

  if (!fileExists(statusFile)) {
    return {
      exists: false,
      message: null
    };
  }

  try {
    const status = readJson(statusFile);
    return {
      exists: true,
      data: status,
      isValid: validateStatusSchema(status)
    };
  } catch (error) {
    return {
      exists: true,
      data: null,
      isValid: false,
      error: error.message
    };
  }
}
```

### 2. 状态文件不存在时

如果 `docs/ccteam/project-status.json` 不存在：

```
╔══════════════════════════════════════════════════════════════╗
║                    未找到进行中的项目                           ║
╚══════════════════════════════════════════════════════════════╝

当前目录下没有检测到 CCTeam 项目状态文件。

请使用以下命令开始：

  /ccteam-new      - 创建新项目，启动完整开发流程
  /ccteam-feature  - 为现有项目添加新功能
  /ccteam          - 进入项目向导，选择操作类型

如果您确信项目存在，请检查：
  1. 当前工作目录是否正确
  2. docs/ccteam/project-status.json 文件是否存在
  3. 文件是否损坏（可尝试手动检查JSON格式）
```

### 3. 状态文件存在时 - 显示项目信息

```
╔══════════════════════════════════════════════════════════════╗
║                       恢复项目                                 ║
╚══════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────┐
│ 项目信息                                                      │
├─────────────────────────────────────────────────────────────┤
│ 项目名称：{{projectName}}                                     │
│ 项目类型：{{projectType}}                                     │
│ 创建时间：{{createdAt}}                                       │
│ 上次更新：{{updatedAt}}                                       │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ 当前状态                                                      │
├─────────────────────────────────────────────────────────────┤
│ 当前阶段：[{{currentStage.id}}] {{currentStage.displayName}} │
│ 阶段状态：{{currentStage.status}}                            │
│ 负责角色：{{currentStage.assignedAgent}}                     │
│ 开始时间：{{currentStage.startedAt}}                         │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ 进度概览                                                      │
├─────────────────────────────────────────────────────────────┤
│ 进度：[████████░░░░░░░░░░░░] 40%                             │
│ 已完成：4/10 阶段                                             │
│ 待处理问题：{{issues.length}} 个                              │
└─────────────────────────────────────────────────────────────┘

是否继续此项目？

  1. ✅ 继续 - 从当前状态恢复工作
  2. 📋 查看详情 - 调用 /ccteam-status 查看完整状态
  3. 🔙 回退 - 回退到上一阶段重新开始
  4. ❌ 取消 - 不恢复，保持当前状态

请选择 (1-4)：
```

### 4. 恢复项目上下文

根据用户选择执行恢复：

```javascript
function resumeProject(status, choice) {
  switch (choice) {
    case 1: // 继续
      return executeResume(status);
    case 2: // 查看详情
      return invokeSkill("/ccteam-status");
    case 3: // 回退
      return invokeSkill("/ccteam-stage rollback");
    case 4: // 取消
      return { action: "cancelled" };
  }
}

function executeResume(status) {
  // 1. 读取完整状态
  const fullStatus = loadFullStatus(status);

  // 2. 加载对应智能体
  const agent = loadAgentForStage(fullStatus.currentStage);

  // 3. 恢复工作上下文
  const context = buildResumeContext(fullStatus);

  // 4. 记录日志
  logEvent("PROJECT_RESUMED", {
    projectName: status.projectName,
    stageId: status.currentStage.id,
    timestamp: new Date().toISOString()
  });

  return {
    success: true,
    context: context,
    agent: agent,
    nextAction: getResumeAction(fullStatus)
  };
}
```

#### 4.1 读取状态

```javascript
function loadFullStatus(status) {
  return {
    ...status,
    // 加载阶段产出物
    outputs: loadStageOutputs(status.stages),
    // 加载未解决的Issues
    openIssues: status.issues.filter(i => i.status === "open"),
    // 加载最近的日志
    recentLogs: loadRecentLogs("docs/ccteam/workflow.log", 10)
  };
}
```

#### 4.2 加载智能体

```javascript
function loadAgentForStage(currentStage) {
  const agentMapping = {
    1: "ccteam-pm",      // 需求分析 → 产品经理
    2: "ccteam-tl",      // 技术评审 → 技术负责人
    3: "ccteam-ui",      // UI/UX设计 → UI设计师
    4: "ccteam-dba",     // 数据库设计 → DBA
    5: "ccteam-api",     // API设计 → API工程师
    6: "ccteam-fe",      // 前端开发 → 前端工程师
    7: "ccteam-be",      // 后端开发 → 后端工程师
    8: "ccteam-qa",      // 测试 → 测试工程师
    9: "ccteam-ops",     // 部署上线 → DevOps工程师
    10: "ccteam-doc"     // 文档归档 → 文档工程师
  };

  const agentId = agentMapping[currentStage.id];
  return loadAgent(agentId);
}
```

#### 4.3 恢复工作

```javascript
function buildResumeContext(fullStatus) {
  return {
    // 项目基本信息
    project: {
      name: fullStatus.projectName,
      type: fullStatus.projectType,
      techStack: fullStatus.techStack
    },

    // 当前阶段信息
    currentStage: fullStatus.currentStage,

    // 已完成阶段的产出物
    completedOutputs: fullStatus.outputs.filter(
      o => o.stageId < fullStatus.currentStage.id
    ),

    // 当前阶段已有的产出物
    currentOutputs: fullStatus.outputs.filter(
      o => o.stageId === fullStatus.currentStage.id
    ),

    // 未解决的问题
    openIssues: fullStatus.openIssues,

    // 评审反馈（如果有）
    reviewFeedback: fullStatus.currentStage.reviewResult
  };
}
```

### 5. 状态恢复策略表

根据当前阶段状态采用不同的恢复策略：

| 状态 | 处理方式 | 说明 |
|------|----------|------|
| `pending` | 启动阶段 | 阶段尚未开始，自动调用 `engine.startStage()` |
| `in-progress` | 继续工作 | 阶段进行中，加载上下文继续工作 |
| `review` | 等待评审 | 提示用户等待评审结果，或联系评审人 |
| `rejected` | 处理问题 | 显示评审反馈，引导修复问题后重新提交 |
| `completed` | 进入下一阶段 | 当前阶段已完成，自动启动下一阶段 |

```javascript
function getResumeAction(status) {
  const current = status.currentStage;

  switch (current.status) {
    case "pending":
      return {
        action: "start",
        message: `阶段 "${current.displayName}" 尚未启动，正在为您启动...`,
        execute: () => engine.startStage(current.id)
      };

    case "in-progress":
      return {
        action: "continue",
        message: `继续 "${current.displayName}" 阶段的工作`,
        context: buildWorkContext(status)
      };

    case "review":
      return {
        action: "wait",
        message: `"${current.displayName}" 正在等待评审`,
        reviewer: current.reviewer,
        submittedAt: current.reviewSubmittedAt
      };

    case "rejected":
      return {
        action: "fix",
        message: `"${current.displayName}" 评审未通过，需要修复以下问题`,
        issues: status.openIssues.filter(i => i.stageId === current.id),
        feedback: current.reviewResult
      };

    case "completed":
      const nextStage = getNextPendingStage(status);
      if (nextStage) {
        return {
          action: "next",
          message: `"${current.displayName}" 已完成，正在启动下一阶段...`,
          nextStage: nextStage,
          execute: () => engine.startStage(nextStage.id)
        };
      } else {
        return {
          action: "done",
          message: "项目所有阶段已完成！"
        };
      }
  }
}
```

### 6. 智能恢复提示

根据恢复情况提供智能提示：

#### 继续进行中的工作

```
┌─────────────────────────────────────────────────────────────┐
│ 恢复成功                                                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│ 项目 "{{projectName}}" 已恢复                                 │
│                                                              │
│ 当前阶段：[6] 前端开发                                         │
│ 负责角色：前端工程师 (ccteam-fe)                               │
│                                                              │
│ 上次工作内容：                                                 │
│   - 完成了 3 个组件的开发                                      │
│   - 最后编辑：src/components/Header.tsx                       │
│   - 进度：约 60%                                              │
│                                                              │
│ 待完成事项：                                                   │
│   □ 完成 Footer 组件                                          │
│   □ 完成 Sidebar 组件                                         │
│   □ 编写单元测试                                              │
│                                                              │
│ 提示：使用 /ccteam-stage complete 完成当前阶段                 │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

#### 处理评审反馈

```
┌─────────────────────────────────────────────────────────────┐
│ 需要处理评审反馈                                               │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│ 阶段 "[5] API设计" 评审未通过                                  │
│                                                              │
│ 评审人：代码审查员 (ccteam-cr)                                 │
│ 评审时间：2026-01-28 15:30:00                                 │
│                                                              │
│ 反馈意见：                                                    │
│   1. ❌ API路径设计不符合RESTful规范                           │
│   2. ❌ 缺少错误码定义                                         │
│   3. ⚠️ 建议添加分页参数说明                                   │
│                                                              │
│ 修改建议：                                                    │
│   - 将 /api/getUsers 改为 GET /api/users                     │
│   - 在 openapi.yaml 中添加 errors 部分                        │
│   - 为列表接口添加 page/pageSize 参数说明                      │
│                                                              │
│ 操作：                                                        │
│   1. 根据反馈修改产出物                                        │
│   2. 使用 /ccteam-stage complete 重新提交评审                 │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 7. 错误恢复选项

当恢复过程中遇到错误时提供恢复选项：

```javascript
function handleResumeError(error) {
  const options = [];

  switch (error.type) {
    case "INVALID_STATUS_FILE":
      options.push(
        { command: "/ccteam-new", description: "创建新项目（会覆盖现有状态）" },
        { command: "手动修复", description: "检查并修复 project-status.json" }
      );
      break;

    case "MISSING_OUTPUTS":
      options.push(
        { command: "/ccteam-stage rollback", description: "回退到上一阶段" },
        { command: "手动补充", description: "手动创建缺失的产出物" }
      );
      break;

    case "AGENT_NOT_FOUND":
      options.push(
        { command: "/ccteam-config", description: "检查智能体配置" },
        { command: "手动指定", description: "手动指定负责人继续" }
      );
      break;

    case "CORRUPTED_LOG":
      options.push(
        { command: "重建日志", description: "根据状态文件重建 workflow.log" },
        { command: "忽略日志", description: "忽略日志错误继续恢复" }
      );
      break;
  }

  return {
    error: error,
    options: options,
    message: formatErrorMessage(error)
  };
}
```

#### 错误恢复界面

```
╔══════════════════════════════════════════════════════════════╗
║                      恢复遇到问题                              ║
╚══════════════════════════════════════════════════════════════╝

错误类型：状态文件格式无效
错误详情：project-status.json 中缺少 currentStage 字段

请选择恢复方式：

  1. /ccteam-new
     → 创建新项目（会覆盖现有状态文件）

  2. 手动修复
     → 编辑 docs/ccteam/project-status.json 添加缺失字段

  3. 从备份恢复
     → 检查 docs/ccteam/backups/ 目录是否有可用备份

  4. 联系支持
     → 获取更多帮助

请选择 (1-4)：
```

## 依赖技能

| 技能 | 路径 | 用途 |
|------|------|------|
| 流程引擎 | `skills/workflow/engine.md` | 读取状态、启动阶段 |
| 阶段控制 | `skills/workflow/stage-control.md` | 阶段回退、跳过 |
| 评审关卡 | `skills/workflow/review-gate.md` | 处理评审反馈 |
| 智能体技能 | `skills/agents/*.md` | 加载对应角色 |
