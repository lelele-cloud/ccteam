# CCTeam - 查看项目状态

## 概述

查看当前项目的开发进度和状态，与流程引擎集成提供实时状态报告。

## 使用方法

当用户调用 `/ccteam-status` 时：

### 1. 读取项目状态

读取 `docs/ccteam/project-status.json` 文件：

```javascript
function loadProjectStatus() {
  const statusFile = "docs/ccteam/project-status.json";

  if (!fileExists(statusFile)) {
    return {
      success: false,
      error: "NOT_FOUND",
      message: "未找到项目状态文件，请先使用 /ccteam-new 创建项目"
    };
  }

  const status = readJson(statusFile);
  return {
    success: true,
    data: status
  };
}
```

### 2. 生成状态报告

根据读取的状态数据生成完整报告：

```
╔══════════════════════════════════════════════════════════════╗
║                     CCTeam 项目状态报告                        ║
╚══════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────┐
│ 项目信息                                                      │
├─────────────────────────────────────────────────────────────┤
│ 项目名称：{{projectName}}                                     │
│ 项目类型：{{projectType}}                                     │
│ 项目描述：{{description}}                                     │
│ 创建时间：{{createdAt}}                                       │
│ 更新时间：{{updatedAt}}                                       │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ 当前阶段                                                      │
├─────────────────────────────────────────────────────────────┤
│ 阶段名称：[{{currentStage.id}}] {{currentStage.displayName}} │
│ 阶段状态：{{currentStage.status}}                            │
│ 开始时间：{{currentStage.startedAt}}                         │
│ 负责角色：{{currentStage.assignedAgent}}                     │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ 阶段进度                                                      │
├─────────────────────────────────────────────────────────────┤
│ {{stageProgressList}}                                        │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ 进度统计                                                      │
├─────────────────────────────────────────────────────────────┤
│ 总阶段数：{{metrics.totalStages}}                            │
│ 已完成：{{metrics.completedStages}}                          │
│ 已跳过：{{metrics.skippedStages}}                            │
│ 评审退回：{{metrics.reviewRejections}}                       │
│ 预计完成：{{metrics.estimatedCompletion}}                    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ 待处理事项                                                    │
├─────────────────────────────────────────────────────────────┤
│ {{issuesList}}                                               │
└─────────────────────────────────────────────────────────────┘
```

### 3. 进度可视化

#### 进度条

```javascript
function renderProgressBar(completed, total) {
  const percentage = Math.round((completed / total) * 100);
  const filled = Math.round(percentage / 5); // 20格
  const empty = 20 - filled;

  const bar = "█".repeat(filled) + "░".repeat(empty);
  return `[${bar}] ${percentage}%`;
}
```

示例输出：
```
进度：[████████░░░░░░░░░░░░] 40%
```

#### 阶段图示

```
阶段进度：

  [1] 需求分析      ✅ 已完成 (2026-01-28)
  [2] 技术评审      ✅ 已完成 (2026-01-28)
  [3] UI/UX设计     ✅ 已完成 (2026-01-28)
  [4] 数据库设计    ⏭️ 已跳过
  [5] API设计       ✅ 已完成 (2026-01-28)
  [6] 前端开发      🔄 进行中  ← 当前
  [7] 后端开发      🔄 进行中  (并行)
  [8] 测试          ⏳ 等待中
  [9] 部署上线      ⏳ 等待中
  [10] 文档归档     ⏳ 等待中
```

状态图标说明：

| 状态 | 图标 | 说明 |
|------|------|------|
| `completed` | ✅ | 阶段已完成并通过评审 |
| `in-progress` | 🔄 | 阶段正在进行中 |
| `pending` | ⏳ | 阶段等待开始 |
| `review` | 📋 | 阶段已完成，等待评审 |
| `rejected` | ❌ | 评审未通过，需修改 |
| `skipped` | ⏭️ | 阶段已跳过 |

### 4. 快捷操作提示

根据当前状态智能提示可用操作：

```javascript
function getSuggestedActions(status) {
  const actions = [];
  const current = status.currentStage;

  // 根据当前阶段状态提示操作
  switch (current.status) {
    case "pending":
      actions.push({
        command: `/ccteam-stage start ${current.name}`,
        description: `启动 ${current.displayName} 阶段`
      });
      break;

    case "in-progress":
      actions.push({
        command: `/ccteam-stage complete`,
        description: `完成 ${current.displayName} 阶段并提交评审`
      });
      break;

    case "review":
      actions.push({
        command: `等待评审`,
        description: `${current.displayName} 正在评审中`
      });
      break;

    case "rejected":
      actions.push({
        command: `/ccteam-resume`,
        description: `修复问题后继续 ${current.displayName} 阶段`
      });
      break;
  }

  // 如果有未解决的Issues
  if (status.issues.filter(i => i.status === "open").length > 0) {
    actions.push({
      command: `/ccteam-issues`,
      description: `查看并处理 ${status.issues.length} 个待处理问题`
    });
  }

  // 通用操作
  actions.push(
    { command: `/ccteam-stage status`, description: `查看详细阶段状态` },
    { command: `/ccteam-resume`, description: `恢复项目工作` }
  );

  return actions;
}
```

#### 快捷操作输出示例

```
┌─────────────────────────────────────────────────────────────┐
│ 快捷操作                                                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│ 根据当前状态，您可以执行以下操作：                               │
│                                                              │
│ 1. /ccteam-stage complete                                    │
│    → 完成前端开发阶段并提交评审                                 │
│                                                              │
│ 2. /ccteam-issues                                            │
│    → 查看并处理 2 个待处理问题                                  │
│                                                              │
│ 3. /ccteam-resume                                            │
│    → 恢复项目工作                                              │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 5. 产出物清单

显示各阶段产出物及状态：

```
┌─────────────────────────────────────────────────────────────┐
│ 产出物清单                                                    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│ 阶段1 - 需求分析：                                            │
│   ✅ docs/ccteam/01-requirements/prd.md                      │
│   ✅ docs/ccteam/01-requirements/user-stories.md             │
│                                                              │
│ 阶段2 - 技术评审：                                            │
│   ✅ docs/ccteam/02-architecture/tech-design.md              │
│   ✅ docs/ccteam/02-architecture/risk-assessment.md          │
│                                                              │
│ 阶段3 - UI/UX设计：                                          │
│   ✅ docs/ccteam/03-design/design-system.md                  │
│   ✅ docs/ccteam/03-design/wireframes/                       │
│                                                              │
│ 阶段5 - API设计：                                            │
│   🔄 docs/ccteam/05-api/openapi.yaml                         │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 依赖技能

| 技能 | 路径 | 用途 |
|------|------|------|
| 流程引擎 | `skills/workflow/engine.md` | 读取项目状态 |
| 阶段控制 | `skills/workflow/stage-control.md` | 获取阶段详情 |
